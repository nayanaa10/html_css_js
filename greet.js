// 'export default' means: this is the MAIN thing this file provides
// A file can only have ONE default export
export default function greet(name) {
  return `Hello, ${name}!`;   // template literal — fills in the name
}
 
